# แบบฝึกหัด - Introduction & VertiPaq Engine

## 📚 เอกสารแบบฝึกหัดสำหรับการฝึกปฏิบัติ

ไฟล์นี้รวบรวมแบบฝึกหัดแบบ Step-by-Step สำหรับโมดูล Introduction & VertiPaq Engine โดยรวม Code Examples และคำอธิบายที่ละเอียด

---

## 🎯 แบบฝึกหัดที่ 1: ทำความรู้จักกับ Power BI Desktop Views

### วัตถุประสงค์

เรียนรู้และเข้าใจ Views หลักๆ ใน Power BI Desktop ที่ใช้ในการสร้างและจัดการ Semantic Model

---

### ขั้นตอนที่ 1: เปิด Power BI Desktop

**Step 1.1:** เปิด Power BI Desktop ขึ้นมา

**Step 1.2:** สังเกต View หลักๆ ที่มีใน Power BI Desktop:
- **Report View** (ไอคอนรูปกราฟ ด้านซ้าย)
- **Data View** (ไอคอนรูปตาราง ด้านซ้าย)
- **Model View** (ไอคอนรูปแผนผัง ด้านซ้าย)
- **DAX Query View** (ไอคอนรูปแฟลช ด้านซ้าย) - มีใน Power BI Desktop เวอร์ชันใหม่
- **TMDL View** (ไอคอนรูปโค้ด ด้านซ้าย) - มีใน Power BI Desktop เวอร์ชันใหม่

---

### ขั้นตอนที่ 2: สำรวจ Report View

**Step 2.1:** คลิกที่ **Report View** (ไอคอนรูปกราฟ)

**Step 2.2:** สังเกต UI:
- **Visualizations pane** (ด้านขวา) - สำหรับเลือก Visual Types
- **Fields pane** (ด้านขวา) - แสดง Tables, Columns, Measures
- **Canvas** (ตรงกลาง) - พื้นที่สำหรับสร้าง Visuals

**Step 2.3:** เข้าใจว่า Report View ใช้สำหรับ:
- สร้าง Reports และ Visualizations
- ออกแบบ Dashboard
- จัดรูปแบบ Reports

---

### ขั้นตอนที่ 3: สำรวจ Data View

**Step 3.1:** คลิกที่ **Data View** (ไอคอนรูปตาราง)

**Step 3.2:** สังเกต UI:
- แสดงข้อมูลในตารางแบบตาราง (Grid)
- สามารถดูข้อมูลแต่ละตารางได้
- แสดง Data Type ของแต่ละ Column

**Step 3.3:** เข้าใจว่า Data View ใช้สำหรับ:
- ดูข้อมูลในตาราง (Raw Data)
- ตรวจสอบ Data Types
- ดู Sample Data ก่อนสร้าง Visuals

---

### ขั้นตอนที่ 4: สำรวจ Model View

**Step 4.1:** คลิกที่ **Model View** (ไอคอนรูปแผนผัง)

**Step 4.2:** สังเกต UI:
- แสดง Tables ทั้งหมดเป็น Shapes
- แสดง Relationships เป็นเส้นเชื่อมระหว่าง Tables
- สามารถลากเพื่อจัดเรียง Tables ได้

**Step 4.3:** เข้าใจว่า Model View ใช้สำหรับ:
- จัดการ Semantic Model และ Relationships
- ดูโครงสร้าง Data Model
- จัดเรียงและจัดระเบียบ Tables

---

### ขั้นตอนที่ 5: สำรวจ DAX Query View (ถ้ามี)

**Step 5.1:** คลิกที่ **DAX Query View** (ไอคอนรูปแฟลช)

**Step 5.2:** สังเกต UI:
- มี Query Editor สำหรับเขียน DAX Queries
- มีปุ่ม **Run** สำหรับรัน Query

**Step 5.3:** ทดสอบเขียน DAX Query พื้นฐาน:

```dax
EVALUATE
'DimProduct'
```

**Step 5.4:** กด **Run** เพื่อรัน Query และดูผลลัพธ์

**Step 5.5:** เข้าใจว่า DAX Query View ใช้สำหรับ:
- เขียนและทดสอบ DAX Queries
- Debug DAX Queries
- วิเคราะห์ข้อมูลด้วย DAX

---

### ขั้นตอนที่ 6: สำรวจ TMDL View (ถ้ามี)

**Step 6.1:** คลิกที่ **TMDL View** (ไอคอนรูปโค้ด)

**Step 6.2:** สังเกต UI:
- แสดง Tabular Model Definition Language (TMDL)
- เป็น Text-based Representation ของ Semantic Model

**Step 6.3:** เข้าใจว่า TMDL View ใช้สำหรับ:
- จัดการ Model ผ่าน Tabular Model Definition Language
- ดูโครงสร้าง Model ในรูปแบบ Text
- แก้ไข Model ผ่าน TMDL (สำหรับ Advanced Users)

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **Views หลักใน Power BI Desktop มีอะไรบ้าง?**
   - **คำตอบ:** Report View, Data View, Model View, DAX Query View, TMDL View

2. **Report View ใช้ทำอะไร?**
   - **คำตอบ:** ใช้สร้าง Reports และ Visualizations

3. **Model View ใช้ทำอะไร?**
   - **คำตอบ:** ใช้จัดการ Semantic Model และ Relationships

---

## 🎯 แบบฝึกหัดที่ 2: เข้าใจ Power BI Process (Formula Engine vs Storage Engine)

### วัตถุประสงค์

เข้าใจการทำงานของ Formula Engine (FE) และ Storage Engine (SE) ใน Power BI และเข้าใจว่าทำงานร่วมกันอย่างไร

---

### ขั้นตอนที่ 1: เข้าใจ Formula Engine (FE)

**Step 1.1:** อ่านและเข้าใจหน้าที่ของ Formula Engine:

**Formula Engine (FE)** ทำหน้าที่:
- วิเคราะห์และประมวลผลคำสั่ง DAX หรือ MDX ที่ผู้ใช้ส่งมา
- แปลความหมายของคำสั่ง (Parse)
- ตรวจสอบไวยากรณ์ (Syntax Check)
- วางแผนการดำเนินการ (Query Plan)
- ถ้าต้องการข้อมูลเพิ่มเติม จะส่งคำขอไปยัง Storage Engine
- ส่งคืนผลลัพธ์สุดท้ายให้กับผู้ใช้หลังจากประมวลผลเสร็จสิ้น

**Step 1.2:** เข้าใจว่า Formula Engine คำนวณผลลัพธ์ที่ซับซ้อน:
- ใช้ข้อมูลที่ได้จาก Storage Engine
- ประมวลผลด้วย Formulas และ Calculations
- ส่งคืนผลลัพธ์สุดท้าย

---

### ขั้นตอนที่ 2: เข้าใจ Storage Engine (SE)

**Step 2.1:** อ่านและเข้าใจหน้าที่ของ Storage Engine:

**Storage Engine (SE)** ทำหน้าที่:
- จัดการการดึงข้อมูลจากแหล่งที่เก็บข้อมูล (VertiPaq Engine)
- รับคำขอจาก Formula Engine
- ดึงข้อมูลที่จำเป็นจากหน่วยความจำหรือดิสก์
- ใช้เทคนิคการจัดเก็บข้อมูลแบบคอลัมน์ (Columnar Storage) และการบีบอัดข้อมูล (Compression) เพื่อเพิ่มประสิทธิภาพ
- ส่งข้อมูลที่ดึงมาให้กับ Formula Engine เพื่อประมวลผลต่อ

**Step 2.2:** เข้าใจว่า Storage Engine คือ VertiPaq Engine:
- VertiPaq Engine เป็น Storage Engine ที่ใช้ใน Power BI
- ใช้ Columnar Storage Architecture
- ใช้ Data Compression เทคนิคขั้นสูง

---

### ขั้นตอนที่ 3: เข้าใจการทำงานร่วมกัน

**Step 3.1:** เข้าใจกระบวนการทำงาน:

**ขั้นตอนการทำงาน:**
1. ผู้ใช้ส่งคำสั่ง DAX (เช่น `SUM(FactSales[SalesAmount])`)
2. **Formula Engine** รับคำสั่งและวิเคราะห์:
   - แปลความหมายของคำสั่ง
   - วางแผนการดำเนินการ
   - ระบุว่าต้องการข้อมูลอะไรบ้าง
3. **Formula Engine** ส่งคำขอไปยัง **Storage Engine**:
   - ขอข้อมูล SalesAmount จาก FactSales Table
4. **Storage Engine** ดึงข้อมูล:
   - ใช้ VertiPaq Engine ดึงข้อมูล SalesAmount
   - ใช้ Columnar Storage และ Compression เพื่อเพิ่มประสิทธิภาพ
   - ส่งข้อมูลกลับให้ Formula Engine
5. **Formula Engine** ประมวลผล:
   - คำนวณ SUM ของ SalesAmount
   - ส่งคืนผลลัพธ์ให้ผู้ใช้

**Step 3.2:** เข้าใจความสำคัญ:
- Formula Engine และ Storage Engine ทำงานร่วมกัน
- Formula Engine = Brain (คิดและคำนวณ)
- Storage Engine = Muscle (ดึงข้อมูลเร็ว)

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **Formula Engine (FE) ทำหน้าที่อะไร?**
   - **คำตอบ:** วิเคราะห์และประมวลผลคำสั่ง DAX/MDX, วางแผนการดำเนินการ, และคำนวณผลลัพธ์

2. **Storage Engine (SE) ทำหน้าที่อะไร?**
   - **คำตอบ:** ดึงข้อมูลจาก VertiPaq Engine, ใช้ Columnar Storage และ Compression เพื่อเพิ่มประสิทธิภาพ

3. **Formula Engine และ Storage Engine ทำงานร่วมกันอย่างไร?**
   - **คำตอบ:** Formula Engine ส่งคำขอไปยัง Storage Engine, Storage Engine ดึงข้อมูลและส่งกลับมาให้ Formula Engine, Formula Engine ประมวลผลและส่งผลลัพธ์ให้ผู้ใช้

---

## 🎯 แบบฝึกหัดที่ 3: เข้าใจ Low Cardinality และ High Cardinality

### วัตถุประสงค์

เข้าใจความแตกต่างระหว่าง Low Cardinality และ High Cardinality และเข้าใจผลกระทบต่อ Performance

---

### ขั้นตอนที่ 1: วิเคราะห์ข้อมูลตัวอย่าง

**Step 1.1:** ดูข้อมูลตัวอย่างตารางนักเรียน (มี 10,000 แถว):

| ชื่อ | เพศ | เกรด |
|------|-----|------|
| สมชาย | ชาย | A |
| สมหญิง | หญิง | B |
| วิชัย | ชาย | A |
| วิไล | หญิง | C |
| ... (อีก 9,996 แถว) | ... | ... |

**Step 1.2:** คำนวณ Cardinality ของแต่ละคอลัมน์:

1. **"เพศ" Column:**
   - ค่าที่ไม่ซ้ำกัน: ชาย, หญิง = 2 ค่า
   - **Cardinality = 2** (ต่ำมาก)
   - แต่ละค่าซ้ำ ~5,000 ครั้ง (10,000 ÷ 2)
   - Dictionary Encoding เก็บแค่ 2 ค่า → ประหยัดพื้นที่มาก

2. **"เกรด" Column:**
   - ค่าที่ไม่ซ้ำกัน: A, B, C, D, F = 5 ค่า
   - **Cardinality = 5** (ต่ำ)
   - แต่ละค่าซ้ำ ~2,000 ครั้ง (10,000 ÷ 5)
   - Dictionary Encoding เก็บแค่ 5 ค่า → ประหยัดพื้นที่

3. **"ชื่อ" Column:**
   - แต่ละแถวมีชื่อไม่ซ้ำกันเลย
   - ค่าที่ไม่ซ้ำกัน = 10,000 ค่า
   - **Cardinality = 10,000** (สูงมาก - เท่ากับจำนวนแถวทั้งหมด)
   - Dictionary Encoding ต้องเก็บ 10,000 ค่า → ใช้พื้นที่มาก

---

### ขั้นตอนที่ 2: จำแนก Low Cardinality และ High Cardinality

**Step 2.1:** จำแนก Low Cardinality:
- **"เพศ"**: Cardinality = 2 (ต่ำมาก) → **Low Cardinality** ✅ 
  - แต่ละค่าซ้ำ ~5,000 ครั้ง → Dictionary Encoding เก็บแค่ 2 ค่า → ประหยัดพื้นที่มาก
  
- **"เกรด"**: Cardinality = 5 (ต่ำ) → **Low Cardinality** ✅ 
  - แต่ละค่าซ้ำ ~2,000 ครั้ง → Dictionary Encoding เก็บแค่ 5 ค่า → ประหยัดพื้นที่

**Step 2.2:** จำแนก High Cardinality:
- **"ชื่อ"**: Cardinality = 10,000 (เท่ากับจำนวนแถวทั้งหมด) → **High Cardinality** ❌ 
  - ไม่มีค่าซ้ำกันเลย → Dictionary Encoding ต้องเก็บ 10,000 ค่า → ใช้พื้นที่มาก

**Step 2.3:** เข้าใจหลักการ:
- **Low Cardinality** = ค่าซ้ำกันมาก → Cardinality ต่ำ (น้อยกว่าจำนวนแถวมาก) → ดี ✅
- **High Cardinality** = ค่าซ้ำกันน้อยหรือไม่มี → Cardinality สูง (ใกล้เคียงหรือเท่ากับจำนวนแถว) → ไม่ดี ❌

---

### ขั้นตอนที่ 3: เข้าใจผลกระทบต่อ Performance

**Step 3.1:** เข้าใจว่าทำไม Low Cardinality ถึงดีกว่า:

1. **Dictionary Encoding ทำงานได้ดี:**
   - Dictionary Encoding จะสร้าง Dictionary ของค่าที่ไม่ซ้ำกัน
   - ถ้ามีค่าซ้ำกันมาก (Low Cardinality) → Dictionary จะเล็กมาก
   - เก็บแค่ Reference ID แทนค่าจริง → ประหยัดพื้นที่

2. **RLE Encoding ทำงานได้ดี:**
   - RLE (Run-Length Encoding) จะบีบอัดค่าที่ซ้ำกันเรียงติดกัน
   - ถ้ามีค่าซ้ำกันมาก (Low Cardinality) → RLE จะบีบอัดได้ดี
   - ลดขนาดข้อมูล → ประหยัด Memory → เพิ่ม Performance

3. **ลดขนาดข้อมูล:**
   - เมื่อ Cardinality ต่ำ → ขนาดข้อมูลเล็กลง
   - ใช้ Memory น้อยลง → Query ทำงานเร็วขึ้น

**Step 3.2:** เข้าใจปัญหาของ High Cardinality:

1. **Dictionary Encoding ทำงานได้ไม่ดี:**
   - Dictionary จะใหญ่เท่ากับจำนวนค่าทั้งหมด
   - ไม่ประหยัดพื้นที่

2. **RLE Encoding ทำงานได้ไม่ดี:**
   - ไม่มีค่าซ้ำกัน → RLE ไม่สามารถบีบอัดได้
   - ขนาดข้อมูลเท่ากับข้อมูลดั้งเดิม

3. **ใช้ Memory มาก:**
   - ข้อมูลไม่ถูกบีบอัด → ใช้ Memory มาก
   - Query ทำงานช้าลง

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **คอลัมน์ไหนมี Low Cardinality?**
   - **คำตอบ:** "เพศ" (Cardinality = 2) และ "เกรด" (Cardinality = 3)

2. **คอลัมน์ไหนมี High Cardinality?**
   - **คำตอบ:** "ชื่อ" (Cardinality = 4 - เท่ากับจำนวนแถว)

3. **ทำไม Low Cardinality ถึงดีกว่า High Cardinality?**
   - **คำตอบ:** Low Cardinality ทำให้ Dictionary Encoding และ RLE Encoding ทำงานได้ดี ลดขนาดข้อมูล ใช้ Memory น้อย และเพิ่ม Performance

---

## 🎯 แบบฝึกหัดที่ 4: ลด Cardinality ในข้อมูลจริง

### วัตถุประสงค์

เรียนรู้วิธีลด Cardinality ในข้อมูลจริง โดยใช้ตัวอย่าง DateTime

---

### ขั้นตอนที่ 1: วิเคราะห์ปัญหาของ DateTime

**Step 1.1:** ดูข้อมูลตัวอย่างที่มี DateTime แบบเต็มรูปแบบ:

**ตารางการขาย (มี 50,000 แถว):**

| รหัสใบเสร็จ | เวลาที่ซื้อ (DateTime) |
|------------|----------------------|
| INV-000001 | 2024-01-15 10:23:45.123 |
| INV-000002 | 2024-01-15 10:23:45.456 |
| INV-000003 | 2024-01-15 10:23:45.789 |
| INV-000004 | 2024-01-15 10:23:46.012 |
| ... (อีก 49,996 แถว) | ... |

**Step 1.2:** คำนวณ Cardinality:
- แต่ละแถวมีค่า DateTime ที่แตกต่างกัน (แตกต่างกันที่ Milliseconds)
- จำนวนค่าที่ไม่ซ้ำกัน = 50,000 (เท่ากับจำนวนแถวทั้งหมด)
- **Cardinality = 50,000** (สูงมาก ❌)

**Step 1.3:** วิเคราะห์ปัญหา:
- **Cardinality สูงมาก** → เท่ากับจำนวนแถวทั้งหมด (50,000 แถว = 50,000 Timestamp)
- **แต่ละแถวมีค่าไม่ซ้ำกันเลย** → Dictionary Encoding ต้องเก็บ 50,000 ค่า → ใช้พื้นที่มากมาก
- **ไม่มีค่าซ้ำกัน** → RLE Encoding ไม่สามารถบีบอัดได้เลย
- **ใช้พื้นที่มาก** → ใช้ Memory มาก → Performance ต่ำ

---

### ขั้นตอนที่ 2: แก้ไขโดยการแยก DateTime

**Step 2.1:** แยก DateTime ออกเป็น Date และ Time:

**วิธีที่ 1: แยกเป็น Date และ Time แยกกัน**

**ตารางการขาย (มี 50,000 แถว):**

| รหัสใบเสร็จ | วันที่ซื้อ | เวลาที่ซื้อ |
|------------|-----------|------------|
| INV-000001 | 2024-01-15 | 10:23:45.123 |
| INV-000002 | 2024-01-15 | 10:23:45.456 |
| INV-000003 | 2024-01-15 | 10:23:45.789 |
| ... (อีก 49,997 แถว) | ... | ... |

**ผลลัพธ์:**
- **วันที่ซื้อ**: Cardinality ≈ 365 ค่า (ประมาณ 1 ปี) → แต่ละค่าซ้ำ ~137 ครั้ง (50,000 ÷ 365) ✅
- **เวลาที่ซื้อ**: Cardinality ยังสูงอยู่ (แตกต่างกันที่ Milliseconds) → ยังไม่ดีเท่าที่ควร

**วิธีที่ 2: แยกเป็น Date และ Time Category (แนะนำ)**

**ตารางการขาย (มี 50,000 แถว):**

| รหัสใบเสร็จ | วันที่ซื้อ | ช่วงเวลา |
|------------|-----------|---------|
| INV-000001 | 2024-01-15 | เช้า |
| INV-000002 | 2024-01-15 | เช้า |
| INV-000003 | 2024-01-15 | เช้า |
| INV-000004 | 2024-01-15 | บ่าย |
| ... (อีก 49,996 แถว) | ... | ... |

**ผลลัพธ์:**
- **วันที่ซื้อ**: Cardinality = 365 ค่า (ประมาณ 1 ปี) → แต่ละค่าซ้ำ ~137 ครั้ง ✅
- **ช่วงเวลา**: Cardinality = 3-4 ค่า (เช้า, บ่าย, เย็น, กลางคืน) → แต่ละค่าซ้ำ ~12,500-16,667 ครั้ง ✅
- Dictionary Encoding เก็บแค่ 365 + 4 ค่า แทนที่จะเป็น 50,000 ค่า → **ประหยัดพื้นที่มาก**

---

### ขั้นตอนที่ 3: นำไปใช้ใน Power Query

**Step 3.1:** เปิด Power Query Editor ใน Power BI Desktop

**Step 3.2:** แยก DateTime ด้วย Power Query M:

```m
let
    Source = Table.FromRows(...),
    #"Changed Type" = Table.TransformColumnTypes(Source, {{"OrderDateTime", type datetime}}),
    #"Added Date Column" = Table.AddColumn(#"Changed Type", "OrderDate", each Date.From([OrderDateTime])),
    #"Added Time Column" = Table.AddColumn(#"Added Date Column", "OrderTime", each Time.From([OrderDateTime])),
    #"Removed Columns" = Table.RemoveColumns(#"Added Time Column", {"OrderDateTime"})
in
    #"Removed Columns"
```

**อธิบายแต่ละขั้นตอน:**
- `Date.From([OrderDateTime])` → แยก Date ออกจาก DateTime
- `Time.From([OrderDateTime])` → แยก Time ออกจาก DateTime
- `Table.RemoveColumns(...)` → ลบ Column เดิม (OrderDateTime)

**Step 3.3:** Apply Changes และตรวจสอบผลลัพธ์

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **Cardinality ของคอลัมน์ "เวลาที่ซื้อ" ในตัวอย่างคือเท่าไหร่?**
   - **คำตอบ:** Cardinality = 50,000 (สูงมาก ❌ - เท่ากับจำนวนแถวทั้งหมด)

2. **อธิบายปัญหาของคอลัมน์ DateTime แบบเต็มรูปแบบ:**
   - **คำตอบ:** Cardinality สูงมาก (50,000) เท่ากับจำนวนแถวทั้งหมด แต่ละแถวมีค่าไม่ซ้ำกันเลย Dictionary Encoding ต้องเก็บ 50,000 ค่า → ใช้พื้นที่มากมาก และ RLE Encoding ไม่สามารถบีบอัดได้เลย → Performance ต่ำ

3. **แนะนำวิธีลด Cardinality:**
   - **คำตอบ:** แยก DateTime ออกเป็น Date และ Time Category (เช้า, บ่าย, เย็น, กลางคืน) เพื่อลด Cardinality จาก 50,000 เหลือ 365 (Date) + 4 (Time Category) = 369 ค่า → ประหยัดพื้นที่มาก

---

## 🎯 แบบฝึกหัดที่ 5: Sorted Data และ RLE Encoding

### วัตถุประสงค์

เข้าใจว่าทำไม Sorted Data ถึงช่วยเพิ่มประสิทธิภาพ RLE Encoding และเรียนรู้วิธีเรียงข้อมูล

---

### ขั้นตอนที่ 1: เปรียบเทียบข้อมูลที่เรียงและไม่เรียง

**Step 1.1:** ดูข้อมูลชุดที่ 1: **ไม่เรียงลำดับ**

```
┌──────────────┬──────────────┐
│ รหัสใบเสร็จ   │ รหัสสินค้า   │
├──────────────┼──────────────┤
│ INV-001      │ P002        │
│ INV-002      │ P001        │
│ INV-003      │ P002        │
│ INV-004      │ P001        │
│ INV-005      │ P001        │
└──────────────┴──────────────┘
```

**วิเคราะห์:**
- ค่า P001 และ P002 สลับกัน
- ค่าไม่เรียงติดกัน
- RLE Encoding ไม่สามารถบีบอัดได้ดี

**Step 1.2:** ดูข้อมูลชุดที่ 2: **เรียงลำดับ**

```
┌──────────────┬──────────────┐
│ รหัสใบเสร็จ   │ รหัสสินค้า   │
├──────────────┼──────────────┤
│ INV-002      │ P001        │
│ INV-004      │ P001        │
│ INV-005      │ P001        │
│ INV-001      │ P002        │
│ INV-003      │ P002        │
└──────────────┴──────────────┘
```

**วิเคราะห์:**
- P001 เรียงติดกัน 3 ครั้ง
- P002 เรียงติดกัน 2 ครั้ง
- ค่าเรียงติดกัน
- RLE Encoding สามารถบีบอัดได้ดีมาก

---

### ขั้นตอนที่ 2: เข้าใจการทำงานของ RLE Encoding

**Step 2.1:** เข้าใจ RLE Encoding:

**RLE (Run-Length Encoding)** คือการบีบอัดข้อมูลโดย:
- นับว่าค่าซ้ำกันกี่ครั้งติดกัน
- บันทึกแค่ค่าพร้อมจำนวนครั้งที่ซ้ำ
- แทนที่จะเก็บค่าซ้ำๆ กันหลายครั้ง

**ตัวอย่าง:**
- แทนที่จะเก็บ: P001, P001, P001
- RLE บันทึก: (P001, 3) → ประหยัดพื้นที่มาก

**Step 2.2:** เปรียบเทียบ RLE Encoding ระหว่าง 2 ชุดข้อมูล:

**ชุดที่ 1 (ไม่เรียง):**
- P002, P001, P002, P001, P001
- ค่าไม่เรียงติดกัน → RLE ไม่สามารถบีบอัดได้ดี
- ต้องเก็บ: P002, P001, P002, P001, P001 (5 ค่า)

**ชุดที่ 2 (เรียง):**
- P001, P001, P001, P002, P002
- ค่าเรียงติดกัน → RLE สามารถบีบอัดได้ดี
- เก็บแค่: (P001, 3), (P002, 2) (2 ค่า) → ประหยัดพื้นที่มากกว่า

---

### ขั้นตอนที่ 3: เรียงข้อมูลใน Power Query

**Step 3.1:** เปิด Power Query Editor ใน Power BI Desktop

**Step 3.2:** เรียงข้อมูลตาม ProductKey:

```m
let
    Source = Table.FromRows(...),
    #"Sorted Rows" = Table.Sort(Source, {{"ProductKey", Order.Ascending}})
in
    #"Sorted Rows"
```

**อธิบาย:**
- `Table.Sort(...)` → เรียงข้อมูล
- `{{"ProductKey", Order.Ascending}}` → เรียงตาม ProductKey แบบ Ascending (น้อยไปมาก)

**Step 3.3:** เรียงข้อมูลหลายคอลัมน์ (Multi-Column Sort):

```m
let
    Source = Table.FromRows(...),
    #"Sorted Rows" = Table.Sort(Source, {
        {"ProductKey", Order.Ascending},
        {"OrderDateKey", Order.Ascending}
    })
in
    #"Sorted Rows"
```

**อธิบาย:**
- เรียงตาม ProductKey ก่อน
- ถ้า ProductKey เท่ากัน จะเรียงตาม OrderDateKey

**Step 3.4:** Apply Changes และตรวจสอบผลลัพธ์

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **ชุดข้อมูลไหนจะทำให้ RLE Encoding มีประสิทธิภาพมากกว่า?**
   - **คำตอบ:** ชุดที่ 2 (ข้อมูลที่เรียงลำดับ) เพราะค่าซ้ำกันเรียงติดกัน ทำให้ RLE สามารถบีบอัดได้ดี

2. **ทำไม Sorted Data ถึงช่วยเพิ่มประสิทธิภาพ RLE Encoding?**
   - **คำตอบ:** เพราะเมื่อข้อมูลเรียงลำดับ ค่าซ้ำกันจะอยู่ติดกัน ทำให้ RLE สามารถนับค่าซ้ำกันและบันทึกเป็น (ค่า, จำนวนครั้ง) แทนที่จะเก็บค่าซ้ำๆ กันหลายครั้ง ซึ่งประหยัดพื้นที่มาก

3. **ควรเรียงข้อมูลตามอะไรบ้าง?**
   - **คำตอบ:** ควรเรียงข้อมูลตาม Foreign Keys (เช่น ProductKey) หรือ Date Keys (เช่น OrderDateKey) เพื่อให้ค่าซ้ำกันเรียงติดกัน

---

## 🎯 แบบฝึกหัดที่ 6: Dimension Tables vs Flat Data

### วัตถุประสงค์

เข้าใจความแตกต่างระหว่าง Flat Data และ Star Schema และเข้าใจว่าทำไม Star Schema ถึงดีกว่า

---

### ขั้นตอนที่ 1: วิเคราะห์ Flat Data (ไม่ดี)

**Step 1.1:** ดูตัวอย่าง Flat Data:

**ตารางการขาย (Fact Table - มี 100,000 แถว):**

| รหัสใบเสร็จ | รหัสสินค้า | ชื่อสินค้า | ราคา |
|------------|-----------|-----------|------|
| INV-000001 | P001 | น้ำดื่ม | 20 |
| INV-000002 | P002 | ขนม | 15 |
| INV-000003 | P001 | น้ำดื่ม | 20 |
| INV-000004 | P003 | นม | 25 |
| ... (อีก 99,996 แถว) | ... | ... | ... |

**Step 1.2:** คำนวณ Cardinality:
- **ชื่อสินค้า:** Cardinality ≈ 1,000 (มีสินค้า ~1,000 ชนิด) → สูง ❌
- **ราคา:** Cardinality ≈ 500 (มีราคาแตกต่างกัน ~500 ราคา) → สูง ❌
- **รหัสสินค้า:** Cardinality = 1,000 (ต่ำ ✅)

**Step 1.3:** วิเคราะห์ปัญหา:
- **Attributes เก็บรวมกับ Fact Table** → Cardinality สูง
- **ข้อมูลซ้ำซ้อน** → เช่น "น้ำดื่ม" เก็บซ้ำ ~100 ครั้ง, "20" เก็บซ้ำหลายร้อยครั้ง
- **เมื่อมีข้อมูลมากขึ้น** → Attributes จะซ้ำกันมากขึ้น แต่ยังใช้พื้นที่มาก
- **Dictionary Encoding ทำงานได้ไม่ดี** → ต้องเก็บ Dictionary ที่มี 1,000+ ค่า (ชื่อสินค้า) และ 500+ ค่า (ราคา)

---

### ขั้นตอนที่ 2: วิเคราะห์ Star Schema (ดี)

**Step 2.1:** ดูตัวอย่าง Star Schema:

**ตารางการขาย (Fact Table - มี 100,000 แถว):**

| รหัสใบเสร็จ | รหัสสินค้า |
|------------|-----------|
| INV-000001 | P001 |
| INV-000002 | P002 |
| INV-000003 | P001 |
| INV-000004 | P003 |
| ... (อีก 99,996 แถว) | ... |

**ตารางสินค้า (Dimension Table - มี 1,000 แถว):**

| รหัสสินค้า | ชื่อสินค้า | ราคา |
|-----------|-----------|------|
| P001 | น้ำดื่ม | 20 |
| P002 | ขนม | 15 |
| P003 | นม | 25 |
| ... (อีก 997 แถว) | ... | ... |

**Step 2.2:** คำนวณ Cardinality:
- **Fact Table - รหัสสินค้า:** Cardinality = 1,000 (ต่ำ ✅)
  - แต่ละรหัสซ้ำ ~100 ครั้ง (100,000 ÷ 1,000)
  - เก็บแค่ Integer → ใช้พื้นที่น้อยมาก
  
- **Dimension Table - ชื่อสินค้า:** Cardinality = 1,000 (ต่ำ ✅)
  - เก็บแค่ครั้งเดียว ไม่ซ้ำซ้อน
  
- **Dimension Table - ราคา:** Cardinality ≈ 500 (ต่ำ ✅)
  - เก็บแค่ครั้งเดียว ไม่ซ้ำซ้อน

**Step 2.3:** วิเคราะห์ข้อดี:
- **Fact Table มี Cardinality ต่ำ** → เพราะเก็บแค่ Foreign Key (รหัสสินค้า) → ใช้พื้นที่น้อยมาก
- **Attributes แยกไป Dimension Table** → ลด Cardinality ใน Fact Table
- **Dictionary Encoding ทำงานได้ดี** → Fact Table เก็บแค่ 1,000 ค่า (รหัสสินค้า) แทนที่จะเป็น 1,000 (ชื่อ) + 500 (ราคา) = 1,500 ค่า
- **RLE Encoding ทำงานได้ดี** → เพราะรหัสสินค้าซ้ำกันมากและเรียงติดกัน

---

### ขั้นตอนที่ 3: เปรียบเทียบเมื่อข้อมูลเพิ่มขึ้น

**Step 3.1:** เปรียบเทียบการใช้พื้นที่:

**Flat Data (100,000 แถว):**
- ชื่อสินค้า: เก็บใน Fact Table → เก็บ "น้ำดื่ม" ซ้ำ ~100 ครั้ง → ใช้พื้นที่มาก
- ราคา: เก็บใน Fact Table → เก็บ "20" ซ้ำหลายร้อยครั้ง → ใช้พื้นที่มาก
- Dictionary: ต้องเก็บ 1,000 ค่า (ชื่อสินค้า) + 500 ค่า (ราคา) = 1,500 ค่า

**Star Schema (100,000 แถว):**
- Fact Table: เก็บแค่รหัสสินค้า (Integer) → 1,000 ค่า → ใช้พื้นที่น้อยมาก
- Dimension Table: เก็บชื่อสินค้าและราคาแค่ครั้งเดียว → 1,000 แถว → ไม่ซ้ำซ้อน
- Dictionary: Fact Table เก็บแค่ 1,000 ค่า (รหัสสินค้า) แทนที่จะเป็น 1,500 ค่า

**ผลลัพธ์:** 
- Star Schema ใช้พื้นที่น้อยกว่า (Fact Table เล็กลงมาก)
- Performance ดีกว่า (Dictionary เล็กกว่า, RLE ทำงานได้ดีกว่า)
- เมื่อข้อมูลเพิ่มขึ้น (เช่น 1,000,000 แถว) ความแตกต่างจะชัดเจนมากขึ้น

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **แบบไหนมีประสิทธิภาพมากกว่า?**
   - **คำตอบ:** Star Schema (แบบที่ 2) เพราะ Fact Table มี Cardinality ต่ำกว่า

2. **ทำไม Star Schema ถึงดีกว่า Flat Data?**
   - **คำตอบ:** เพราะ Star Schema แยก Attributes ไป Dimension Table ทำให้ Fact Table มี Cardinality ต่ำ Dictionary Encoding และ RLE Encoding ทำงานได้ดี และใช้พื้นที่น้อยกว่า

3. **เมื่อข้อมูลเพิ่มขึ้น ความแตกต่างจะเป็นอย่างไร?**
   - **คำตอบ:** เมื่อข้อมูลเพิ่มขึ้น Star Schema จะดีกว่า Flat Data มาก เพราะ Dimension Table ไม่ขยายตัว (เก็บแค่ค่าที่ไม่ซ้ำกัน) ในขณะที่ Flat Data ต้องเก็บ Attributes ซ้ำทุกแถว

---

## 🎯 แบบฝึกหัดที่ 7: ใช้ VertiPaq Analyzer

### วัตถุประสงค์

เรียนรู้วิธีใช้ VertiPaq Analyzer เพื่อวิเคราะห์ Cardinality, Size, และ Encoding Method ของแต่ละ Column

---

### ขั้นตอนที่ 1: ติดตั้งและเปิด DAX Studio

**Step 1.1:** ติดตั้ง DAX Studio (ถ้ายังไม่มี):
- ดาวน์โหลดจาก [daxstudio.org](https://daxstudio.org/)
- ติดตั้งตามขั้นตอนปกติ

**Step 1.2:** เปิด Power BI Desktop และเปิดไฟล์ที่มี Semantic Model

**Step 1.3:** เปิด DAX Studio:
- ไปที่ **External Tools** (ใน Power BI Desktop) → **DAX Studio**
- หรือเปิด DAX Studio แยก แล้วเชื่อมต่อกับ Power BI Desktop

---

### ขั้นตอนที่ 2: ใช้ VertiPaq Analyzer

**Step 2.1:** ใน DAX Studio ไปที่แท็บ **VertiPaq Analyzer**

**Step 2.2:** เลือก Tables ที่ต้องการวิเคราะห์:
- คลิกที่ Dropdown เพื่อเลือก Table
- เลือก `FactResellerSales` (หรือ Table อื่นๆ)

**Step 2.3:** ดูผลลัพธ์:

**ตัวอย่างผลลัพธ์ VertiPaq Analyzer:**

**Table: FactResellerSales**

| Column Name | Cardinality | Size (KB) | Encoding |
|-------------|-------------|-----------|----------|
| ProductKey | 397 | 124 | Dictionary |
| OrderDateKey | 1,214 | 489 | Dictionary |
| SalesAmount | 1,234,567 | 3,456 | Value |
| OrderQuantity | 156 | 89 | RLE |
| UnitPrice | 9,876 | 567 | Dictionary |

---

### ขั้นตอนที่ 3: วิเคราะห์ผลลัพธ์

**Step 3.1:** วิเคราะห์ Cardinality:

**Cardinality ต่ำ (ดี ✅):**
- `OrderQuantity`: Cardinality = 156 → ต่ำมาก ✅
- `ProductKey`: Cardinality = 397 → ต่ำ ✅

**Cardinality สูง (ไม่ดี ❌):**
- `SalesAmount`: Cardinality = 1,234,567 → สูงมาก ❌
- `UnitPrice`: Cardinality = 9,876 → สูง ❌

**Step 3.2:** วิเคราะห์ Encoding Method:

**Dictionary Encoding:**
- ใช้กับ Columns ที่มี Cardinality ปานกลาง
- เช่น `ProductKey`, `OrderDateKey`, `UnitPrice`

**RLE Encoding:**
- ใช้กับ Columns ที่มี Low Cardinality และเรียงลำดับ
- เช่น `OrderQuantity` → Cardinality ต่ำ + เรียงลำดับ

**Value Encoding:**
- ใช้กับ Columns ที่มี Cardinality สูงมาก
- เช่น `SalesAmount` → Cardinality สูงมาก

**Step 3.3:** วิเคราะห์ Size:

**Size เล็ก (ดี ✅):**
- `OrderQuantity`: 89 KB → เล็กมาก ✅ (ใช้ RLE Encoding)
- `ProductKey`: 124 KB → เล็ก ✅

**Size ใหญ่ (ไม่ดี ❌):**
- `SalesAmount`: 3,456 KB → ใหญ่ ❌ (Cardinality สูงมาก)

---

### ขั้นตอนที่ 4: ระบุ Columns ที่ควรปรับปรุง

**Step 4.1:** ระบุ Columns ที่มี High Cardinality:

1. **SalesAmount** (Cardinality = 1,234,567):
   - Cardinality สูงมาก ❌
   - ใช้ Value Encoding (ไม่ดีเท่า Dictionary หรือ RLE)
   - Size ใหญ่ (3,456 KB)
   - **ควรปรับปรุง:** ไม่สามารถปรับปรุงได้มาก (เป็น Measures)

2. **UnitPrice** (Cardinality = 9,876):
   - Cardinality สูง
   - ใช้ Dictionary Encoding
   - **ควรปรับปรุง:** พิจารณา Round หรือแยกเป็น Price Bands

**Step 4.2:** Columns ที่ดีอยู่แล้ว:
- `OrderQuantity`: Cardinality ต่ำ + RLE Encoding → ดีมาก ✅
- `ProductKey`: Cardinality ต่ำ + Dictionary Encoding → ดี ✅

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **คอลัมน์ไหนมี Cardinality ต่ำที่สุด?**
   - **คำตอบ:** OrderQuantity (Cardinality = 156)

2. **คอลัมน์ไหนใช้ RLE Encoding?**
   - **คำตอบ:** OrderQuantity เพราะมี Cardinality ต่ำและเรียงลำดับ

3. **คอลัมน์ไหนควรปรับปรุง?**
   - **คำตอบ:** SalesAmount (Cardinality สูงมาก) และ UnitPrice (Cardinality สูง)

---

## 🎯 แบบฝึกหัดที่ 8: ใช้ Tabular Editor

### วัตถุประสงค์

เรียนรู้วิธีใช้ Tabular Editor เพื่อจัดการ Semantic Model ได้เร็วกว่า Power BI Desktop

---

### ขั้นตอนที่ 1: ติดตั้งและเปิด Tabular Editor

**Step 1.1:** ติดตั้ง Tabular Editor (ถ้ายังไม่มี):
- Tabular Editor 2 (Open Source): ดาวน์โหลดจาก [GitHub](https://github.com/TabularEditor/TabularEditor)
- หรือ Tabular Editor 3 (Commercial)

**Step 1.2:** เปิด Power BI Desktop และเปิดไฟล์ที่มี Semantic Model

**Step 1.3:** เปิด Tabular Editor:
- ไปที่ **External Tools** (ใน Power BI Desktop) → **Tabular Editor**
- หรือเปิด Tabular Editor แยก แล้วเชื่อมต่อกับ Power BI Desktop

---

### ขั้นตอนที่ 2: สำรวจ TOM Explorer

**Step 2.1:** ดูโครงสร้าง Semantic Model ใน TOM Explorer:
- จะเห็น Tables ทั้งหมด
- จะเห็น Columns, Measures, Relationships
- จะเห็น Hierarchies, Perspectives

**Step 2.2:** คลิกขยาย Tables:
- คลิกที่ `+` ข้างชื่อ Table เพื่อขยาย
- จะเห็น Columns, Measures, Hierarchies

**Step 2.3:** เลือก Column หรือ Measure:
- คลิกที่ Column หรือ Measure
- จะเห็น Properties Panel ด้านล่าง

---

### ขั้นตอนที่ 3: แก้ไข Properties

**Step 3.1:** แก้ไข Column Properties:

1. เลือก Column ที่ต้องการแก้ไข (เช่น `DimProduct[ProductName]`)
2. ดู Properties Panel ด้านล่าง:
   - **Name**: ชื่อ Column
   - **Data Type**: ประเภทข้อมูล
   - **IsHidden**: ซ่อนใน Report View หรือไม่
   - **Description**: คำอธิบาย

3. แก้ไข Properties:
   - เปลี่ยน `IsHidden` เป็น `true` เพื่อซ่อน Column
   - เพิ่ม `Description` เพื่อ Document Column

**Step 3.2:** แก้ไข Measure Properties:

1. เลือก Measure ที่ต้องการแก้ไข
2. ดู Properties Panel:
   - **Name**: ชื่อ Measure
   - **Expression**: DAX Formula
   - **Format String**: Format ของผลลัพธ์
   - **Description**: คำอธิบาย

3. แก้ไข Properties:
   - เปลี่ยน `Format String` เป็น `#,##0.00` สำหรับตัวเลข
   - เพิ่ม `Description` เพื่อ Document Measure

---

### ขั้นตอนที่ 4: ใช้ Scripting (Advanced)

**Step 4.1:** เข้าใจ Scripting:
- Tabular Editor รองรับ C# Scripting
- สามารถแก้ไขหลาย Items พร้อมกันได้
- เหมาะสำหรับ Bulk Changes

**Step 4.2:** ตัวอย่าง Script: ซ่อน Foreign Keys ทั้งหมด

```csharp
// Set IsHidden = true for all Foreign Keys in Fact tables
foreach(var table in Model.Tables.Where(t => t.Name.StartsWith("Fact")))
{
    foreach(var column in table.Columns.Where(c => c.Name.EndsWith("Key")))
    {
        column.IsHidden = true;
    }
}
```

**ขั้นตอนการรัน Script:**
1. ไปที่ **File** → **New Script** (หรือกด Ctrl+N)
2. พิมพ์ Script ข้างต้น
3. กด **F5** เพื่อรัน Script
4. ตรวจสอบผลลัพธ์

**Step 4.3:** ตัวอย่าง Script: ตั้งค่า Format String สำหรับ Measures

```csharp
// Set Format String for all measures in a table
foreach(var measure in Model.Tables["FactResellerSales"].Measures)
{
    measure.FormatString = "#,##0.00";
}
```

---

### ขั้นตอนที่ 5: Save Changes

**Step 5.1:** Save Changes กลับไป Power BI Desktop:
- ไปที่ **File** → **Save** (หรือกด Ctrl+S)
- หรือคลิกไอคอน Save

**Step 5.2:** ตรวจสอบว่า Changes ถูกบันทึก:
- กลับไป Power BI Desktop
- ตรวจสอบว่า Properties ที่แก้ไขแสดงผลถูกต้อง

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **Tabular Editor ช่วยอะไรได้บ้าง?**
   - **คำตอบ:** จัดการ Metadata ได้เร็วกว่า Power BI Desktop, ตั้งค่า Properties ที่ซับซ้อนได้ง่าย, สร้างและแก้ไข Measures/Columns จำนวนมากได้เร็ว, จัดการ Calculation Groups ได้สะดวก, รองรับ Scripting สำหรับ Automation

2. **อะไรคือข้อดีของการใช้ Tabular Editor แทน Power BI Desktop?**
   - **คำตอบ:** ทำงานได้เร็วขึ้นมาก, จัดการหลาย Items พร้อมกันได้, รองรับ Scripting สำหรับ Automation, เหมาะสำหรับการจัดการ Large Models

3. **Scripting ใน Tabular Editor ใช้ทำอะไรได้บ้าง?**
   - **คำตอบ:** แก้ไข Properties หลาย Items พร้อมกัน (Bulk Changes), Automation Tasks, Export/Import Metadata, และทำงานที่ซับซ้อนได้ง่าย

---

## 📝 สรุป

### ✅ สิ่งที่ควรจำ

1. **Power BI Desktop มี Views หลักๆ หลายตัว** - Report View, Data View, Model View, DAX Query View, TMDL View
2. **Formula Engine และ Storage Engine ทำงานร่วมกัน** - Formula Engine = Brain, Storage Engine = Muscle
3. **VertiPaq Engine เป็น Storage Engine** - ใช้ Columnar Storage และ Compression
4. **Low Cardinality = ดี** - ค่าซ้ำกันมาก, Cardinality ต่ำ → Dictionary Encoding และ RLE Encoding ทำงานได้ดี
5. **High Cardinality = ไม่ดี** - ค่าซ้ำกันน้อย, Cardinality สูง → Dictionary Encoding และ RLE Encoding ทำงานได้ไม่ดี
6. **Sorted Data ช่วยเพิ่มประสิทธิภาพ RLE Encoding** - ค่าซ้ำกันเรียงติดกัน → RLE บีบอัดได้ดี
7. **Dimension Tables ช่วยลด Cardinality ใน Fact Tables** - แยก Attributes ไป Dimension Table → Fact Table มี Cardinality ต่ำ
8. **VertiPaq Analyzer ใช้วิเคราะห์และปรับปรุงประสิทธิภาพ** - ดู Cardinality, Size, Encoding Method
9. **Tabular Editor ช่วยให้การจัดการ Semantic Model เร็วขึ้นมาก** - รองรับ Bulk Changes และ Scripting
10. **Relationships เป็นหัวใจสำคัญของ Semantic Model** - เชื่อม Fact Tables และ Dimension Tables เข้าด้วยกัน

---

## 🔗 เอกสารที่เกี่ยวข้อง

- [README.md](./README.md) - เอกสารหลักของโมดูล
- [CODE-EXAMPLES.md](./CODE-EXAMPLES.md) - Code Examples เพิ่มเติม
- **ต่อไป:** [02-Data-Sources](../02-Data-Sources/README.md)

> **หมายเหตุ**: โมดูลนี้รวมเนื้อหาจาก 01-Introduction, 02-VertiPaq-Engine และ 14-External-Tools-XMLA เข้าด้วยกัน
