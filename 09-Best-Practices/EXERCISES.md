# แบบฝึกหัด - Best Practices

## 📚 เอกสารแบบฝึกหัดสำหรับการฝึกปฏิบัติ

ไฟล์นี้รวบรวมแบบฝึกหัดแบบ Step-by-Step สำหรับโมดูล Best Practices โดยรวม Code Examples และคำอธิบายที่ละเอียด

---

## 🎯 แบบฝึกหัดที่ 1: Naming Conventions

### วัตถุประสงค์

เรียนรู้วิธีตั้งชื่อ Tables, Columns, และ Measures ให้สอดคล้องกันและสื่อความหมาย

---

### ขั้นตอนที่ 1: เข้าใจ Naming Conventions สำหรับ Tables

**Step 1.1:** เข้าใจ Fact Tables:

**Fact Tables** = ตารางที่เก็บ Measures และ Foreign Keys

**Naming Convention:**
- ใช้คำว่า **"Fact"** นำหน้า
- ตามด้วยชื่อ Business Process

**ตัวอย่าง:**
- ✅ `FactResellerSales`
- ✅ `FactInternetSales`
- ✅ `FactSalesQuota`
- ❌ `Sales` (ขาด "Fact")
- ❌ `ResellerSales` (ขาด "Fact")

**Step 1.2:** เข้าใจ Dimension Tables:

**Dimension Tables** = ตารางที่เก็บ Dimension Attributes

**Naming Convention:**
- ใช้คำว่า **"Dim"** นำหน้า
- ตามด้วยชื่อ Dimension

**ตัวอย่าง:**
- ✅ `DimProduct`
- ✅ `DimDate`
- ✅ `DimCustomer`
- ✅ `DimGeography`
- ❌ `Product` (ขาด "Dim")
- ❌ `Date` (อาจสับสนกับ Date Data Type)

---

### ขั้นตอนที่ 2: เข้าใจ Naming Conventions สำหรับ Measures

**Step 2.1:** เข้าใจหลักการ:

**Measures** = ค่าที่ต้องคำนวณด้วย DAX

**Naming Convention:**
- ใช้ชื่อที่ **สื่อความหมาย**
- ใช้ **PascalCase** หรือ **CamelCase**
- ระบุ **Business Context** ถ้าจำเป็น

**ตัวอย่าง:**
- ✅ `Total Sales`
- ✅ `Sales YTD`
- ✅ `Sales - Bikes`
- ✅ `Sales % - Bikes`
- ❌ `Measure1` (ไม่สื่อความหมาย)
- ❌ `Sum1` (ไม่สื่อความหมาย)

**Step 2.2:** ตัวอย่างชื่อ Measures ที่ดี:

**Base Measures:**
- `Total Sales`
- `Total Cost`
- `Total Profit`

**Filtered Measures:**
- `Sales - Bikes`
- `Sales - North America`

**Percentage Measures:**
- `Sales % - Bikes`
- `Profit Margin %`

**Time Intelligence Measures:**
- `Sales YTD`
- `Sales LY`
- `Sales MTD`

---

### ขั้นตอนที่ 3: ปรับชื่อ Tables ใน Semantic Model

**Step 3.1:** ตรวจสอบชื่อ Tables ปัจจุบัน:

1. ไปที่ **Model View**
2. สังเกตชื่อ Tables ทั้งหมด
3. ระบุ Tables ที่ชื่อไม่สอดคล้องกับ Convention

**Step 3.2:** เปลี่ยนชื่อ Tables:

**ตัวอย่าง:**
- ถ้ามี Table ชื่อ `Sales` → เปลี่ยนเป็น `FactSales`
- ถ้ามี Table ชื่อ `Product` → เปลี่ยนเป็น `DimProduct`

**ขั้นตอน:**
1. Right-click ที่ Table
2. เลือก **Rename**
3. พิมพ์ชื่อใหม่ตาม Convention

---

### ขั้นตอนที่ 4: ปรับชื่อ Measures

**Step 4.1:** ตรวจสอบชื่อ Measures:

1. ไปที่ **Fields pane**
2. สังเกตชื่อ Measures ทั้งหมด
3. ระบุ Measures ที่ชื่อไม่สื่อความหมาย

**Step 4.2:** เปลี่ยนชื่อ Measures:

**ตัวอย่าง:**
- ถ้ามี Measure ชื่อ `Sum1` → เปลี่ยนเป็น `Total Sales`
- ถ้ามี Measure ชื่อ `Avg1` → เปลี่ยนเป็น `Average Sales`

**ขั้นตอน:**
1. Right-click ที่ Measure
2. เลือก **Rename**
3. พิมพ์ชื่อใหม่ที่สื่อความหมาย

**หมายเหตุ:** หลังเปลี่ยนชื่อ Measures อาจต้องอัพเดท Visuals ที่ใช้ Measures นี้

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **การตั้งชื่อ Fact Tables ควรใช้รูปแบบไหน?**
   - **คำตอบ:** ใช้คำว่า "Fact" นำหน้า เช่น FactResellerSales

2. **การตั้งชื่อ Dimension Tables ควรใช้รูปแบบไหน?**
   - **คำตอบ:** ใช้คำว่า "Dim" นำหน้า เช่น DimProduct

3. **การตั้งชื่อ Measures ควรใช้รูปแบบไหน?**
   - **คำตอบ:** ใช้ชื่อที่สื่อความหมาย เช่น Total Sales, Sales YTD

---

## 🎯 แบบฝึกหัดที่ 2: จัดระเบียบ Model

### วัตถุประสงค์

เรียนรู้วิธีจัดระเบียบ Semantic Model ให้เป็นระบบและเข้าใจง่าย

---

### ขั้นตอนที่ 1: จัดกลุ่ม Tables ด้วย Display Folder

**Step 1.1:** เข้าใจ Display Folder:

**Display Folder** = วิธีจัดกลุ่ม Tables และ Columns ใน Fields pane

**ประโยชน์:**
- ทำให้ Fields pane เป็นระเบียบ
- เข้าใจง่ายขึ้น

**Step 1.2:** ตั้งค่า Display Folder สำหรับ Tables:

1. ไปที่ **Model View**
2. เลือก Table (เช่น `FactResellerSales`)
3. ไปที่ **Properties pane** → **Display Folder**
4. พิมพ์ชื่อ Folder (เช่น `Fact Tables`)

**ตัวอย่าง:**
- **Fact Tables**: FactResellerSales, FactInternetSales
- **Dimension Tables**: DimProduct, DimDate, DimCustomer
- **Helper Tables**: Measure Selector, All Measures

---

### ขั้นตอนที่ 2: จัดกลุ่ม Measures ด้วย Display Folder

**Step 2.1:** ตั้งค่า Display Folder สำหรับ Measures:

1. เลือก Measure (เช่น `Total Sales`)
2. ไปที่ **Properties pane** → **Display Folder**
3. พิมพ์ชื่อ Folder (เช่น `Sales Measures`)

**ตัวอย่าง:**
- **Sales Measures**: Total Sales, Sales YTD, Sales - Bikes
- **Cost Measures**: Total Cost, Cost YTD
- **Profit Measures**: Total Profit, Profit Margin

---

### ขั้นตอนที่ 3: ตั้งค่า Description

**Step 3.1:** เข้าใจ Description:

**Description** = คำอธิบายสำหรับ Tables, Columns, และ Measures

**ประโยชน์:**
- ช่วยให้ผู้ใช้เข้าใจข้อมูลได้ดีขึ้น
- ใช้สำหรับ Documentation

**Step 3.2:** ตั้งค่า Description สำหรับ Tables:

1. เลือก Table (เช่น `FactResellerSales`)
2. ไปที่ **Properties pane** → **Description**
3. พิมพ์คำอธิบาย (เช่น "Fact table containing reseller sales transactions")

**Step 3.3:** ตั้งค่า Description สำหรับ Measures:

1. เลือก Measure (เช่น `Total Sales`)
2. ไปที่ **Properties pane** → **Description**
3. พิมพ์คำอธิบาย (เช่น "Total sales amount across all transactions")

---

### คำถามเพื่อตรวจสอบความเข้าใจ

1. **Display Folder ใช้ทำอะไร?**
   - **คำตอบ:** จัดกลุ่ม Tables และ Columns ใน Fields pane ให้เป็นระเบียบ

2. **Description ใช้ทำอะไร?**
   - **คำตอบ:** อธิบาย Tables, Columns, และ Measures เพื่อให้ผู้ใช้เข้าใจได้ดีขึ้น

---

## 📝 สรุป

### ✅ สิ่งที่ควรจำ

1. **ใช้ Naming Conventions ที่สอดคล้องกัน** (Fact/Dim Prefix, Descriptive Names)
2. **จัดระเบียบ Model ให้เป็นระบบ** (Display Folders, Descriptions)
3. **ทำ Documentation ให้ครบถ้วน** (Descriptions, Comments)

---

## 🔗 เอกสารที่เกี่ยวข้อง

- [README.md](./README.md) - เอกสารหลักของโมดูล
- [CODE-EXAMPLES.md](./CODE-EXAMPLES.md) - Code Examples เพิ่มเติม
